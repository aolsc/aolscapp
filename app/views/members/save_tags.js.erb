//jQuery('#member_data td[mem_id="<%= @member.id %>"] #current_tags').text("<%=  @member.tags_list.join(', ') %>");
jQuery('#member_data td[mem_id="<%= @member.id %>"] #updatestatus').text('Saved tags!');
